import * as React from "react"
import { cn } from "../../utils"
import { CalendarPrimitive } from "@radix-ui/react-calendar"

const Calendar = CalendarPrimitive.Root
const CalendarTrigger = CalendarPrimitive.Trigger
const CalendarContent = CalendarPrimitive.Content
const CalendarHeader = CalendarPrimitive.Header
const CalendarTitle = CalendarPrimitive.Title
const CalendarPrevious = CalendarPrimitive.Prev
const CalendarNext = CalendarPrimitive.Next
const CalendarGrid = CalendarPrimitive.Grid
const CalendarDay = CalendarPrimitive.Day
const CalendarToday = CalendarPrimitive.Today
const CalendarWeekday = CalendarPrimitive.Weekday

export {
  Calendar,
  CalendarTrigger,
  CalendarContent,
  CalendarHeader,
  CalendarTitle,
  CalendarPrevious,
  CalendarNext,
  CalendarGrid,
  CalendarDay,
  CalendarToday,
  CalendarWeekday,
}